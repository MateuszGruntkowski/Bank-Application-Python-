/**
 * Application configuration.
 * This is the ONLY file you need to edit to customize the bank name.
 */
const config = {
  bankName: "Bank Application",
  bankTagline: "Your money in safe hands",
  teamName: "Team 3",
  currency: "PLN",
  locale: "pl-PL",
};

export default config;
